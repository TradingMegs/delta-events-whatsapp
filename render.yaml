services:
  - type: web
    name: delta-whatsapp
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
    disk:
      name: whatsapp-sessions
      mountPath: /app/sessions
      sizeGB: 1
